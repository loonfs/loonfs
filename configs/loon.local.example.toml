# Example LoonFS CLI configuration for a local embedded profile.
# The CLI reads this file from ~/.loonfs/config.toml; `loon init` writes it.

config_version = 1
default_profile = "local"

[profiles.local]
mode = "embedded"
writer_id = "example-local-writer"

[profiles.local.store]
kind = "local-fs"
root = "./.loonfs-store"
key_prefix = "local-example"
